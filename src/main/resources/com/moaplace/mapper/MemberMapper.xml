<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.moaplace.mapper.MemberMapper">
	<select id="findById" resultType="string">
		SELECT member_id FROM member
		WHERE member_name = #{memberName}, member_email = #{memberEmail}
	</select>
	
	<select id="findByPassword" resultType="string">
		SELECT member_pwd FROM member
		WHERE member_id = #{memberId}, member_email = #{memberEmail} 
	</select>
	
	<select id="login" resultType="string">
		SELECT member_id FROM member
		WHERE member_id = #{memberId}, member_pwd = #{memberPwd}
	</select>
</mapper>